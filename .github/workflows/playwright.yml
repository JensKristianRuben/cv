name: Playwright Tests

on: 
    push:
        branches: [main, master]
    pull_request: 
        branches: [main, master]

jobs:
    jobname:
        timeout-minutes: 60
        runs-on: ubuntu-latest
